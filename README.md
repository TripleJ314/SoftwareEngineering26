# Software Engineering Project Starter Code

The compute engine will find the largest prime number that is less than a given positive integer input n. The system will evaluate integers from 2 up to n-1. The integers are classified as prime if greater than 1, and no positive divisors other than 1 and n.

Example:
For the given input: 10:
The system will output: 7